meta {
  name: N026_RequestBodyMissing
  type: http
  seq: 5
  tags: [
    DOWNSTREAM
  ]
}

post {
  url: {{api-host}}/{{context-path}}
  body: json
  auth: none
}

headers {
  Authorization: {{authorization}}
  Accept: {{acceptHeader}}
  correlationId: {{correlationId}}
}

assert {
  res.status: eq 400
  res.body.code: eq "400"
  res.body.message: eq The JSON payload is invalid
}
