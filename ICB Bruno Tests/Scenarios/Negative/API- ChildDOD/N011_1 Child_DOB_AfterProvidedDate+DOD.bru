meta {
  name: N011_1 Child_DOB_AfterProvidedDate+DOD
  type: http
  seq: 2
}

post {
  url: {{api-host}}/{{context-path}}
  body: json
  auth: inherit
}

headers {
  Authorization: {{authorization}}
  Accept: application/vnd.hmrc.2.0+json
  correlationId: {{correlationId}}
}

body:json {
  {
    "firstName": "Andrew",
    "secondName": "Harris",
    "dateOfBirth": "1990-06-27",
    "nino": "{{generatedNino}}013A",
    "bornOnOrAfter": "2023-06-28"
  }
}

assert {
  res.body.eligible: eq false
  res.headers.correlationid: eq {{correlationId}}
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
